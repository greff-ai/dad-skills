# Plan and run a sprint

With scope arguments, follow `references/sprint/plan.md`, then pass the
resulting sprint key to `references/sprint/pr.md` in the same conversation.
Planning questions and confirmation still apply; no extra confirmation is
needed between the approved plan and execution.

Without scope arguments, plan interactively and stop after reporting the
sprint issue. Do not start code work until the user requests it.
