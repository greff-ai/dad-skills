# Create a task

Follow `references/issue/create-common.md` with type task and the user's context.

Require Scope (in and out) and a checkable Definition of done.
Internal/release-note treatment comes from `issueTypes.task.releaseNotes`;
do not infer it later from prose.
