# Choose an issue type

Classify the user's request, asking once if its intent is unclear:

- bug: existing behavior is broken.
- feature: new user-visible capability.
- task: internal work.

Then follow `references/issue/create-bug.md`,
`references/issue/create-feature.md`, or `references/issue/create-task.md`
with the original context. Classification itself creates nothing.
