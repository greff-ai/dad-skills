---
name: dad-issue
description: Draft and file a bug, feature, or task from the user's request.
---

When the user requests this workflow, locate the sibling `dad` skill through
the host's skill metadata (or `DAD_SKILL_DIR`). Read
`references/issue/create.md` there and follow it with the user's arguments
and conversation context.
