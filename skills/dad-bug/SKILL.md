---
name: dad-bug
description: Draft and file a bug with reproduction, expected behavior, and actual behavior.
---

When the user requests this workflow, locate the sibling `dad` skill through
the host's skill metadata (or `DAD_SKILL_DIR`). Read
`references/issue/create-bug.md` there and follow it with the user's arguments
and conversation context.
