---
name: dad-feature
description: Draft and file a feature with a user story and acceptance criteria.
---

When the user requests this workflow, locate the sibling `dad` skill through
the host's skill metadata (or `DAD_SKILL_DIR`). Read
`references/issue/create-feature.md` there and follow it with the user's arguments
and conversation context.
