---
name: dad-sprint-update
description: Turn requested sprint changes or review feedback into tracked issue work.
---

When the user requests this workflow, locate the sibling `dad` skill through
the host's skill metadata (or `DAD_SKILL_DIR`). Read
`references/sprint/update.md` there and follow it with the user's arguments
and conversation context.
