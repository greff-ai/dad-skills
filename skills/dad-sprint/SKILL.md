---
name: dad-sprint
description: Plan a sprint and run its issues through an open sprint pull request.
---

When the user requests this workflow, locate the sibling `dad` skill through
the host's skill metadata (or `DAD_SKILL_DIR`). Read
`references/sprint/run.md` there and follow it with the user's arguments
and conversation context.
