---
name: dad-task
description: Draft and file internal work with scope and a definition of done.
---

When the user requests this workflow, locate the sibling `dad` skill through
the host's skill metadata (or `DAD_SKILL_DIR`). Read
`references/issue/create-task.md` there and follow it with the user's arguments
and conversation context.
