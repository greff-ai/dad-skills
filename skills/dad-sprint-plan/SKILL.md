---
name: dad-sprint-plan
description: Plan a sprint or add scope to an existing sprint.
---

When the user requests this workflow, locate the sibling `dad` skill through
the host's skill metadata (or `DAD_SKILL_DIR`). Read
`references/sprint/plan.md` there and follow it with the user's arguments
and conversation context.
