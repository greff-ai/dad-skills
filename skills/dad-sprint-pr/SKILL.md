---
name: dad-sprint-pr
description: Run or resume a planned sprint and open its pull request for human review.
---

When the user requests this workflow, locate the sibling `dad` skill through
the host's skill metadata (or `DAD_SKILL_DIR`). Read
`references/sprint/pr.md` there and follow it with the user's arguments
and conversation context.
